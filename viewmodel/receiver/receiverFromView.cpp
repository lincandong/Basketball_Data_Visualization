#include "receiverFromView.h"
#include "../viewmodel.h"
receiverFromView::receiverFromView(viewModel* ptr)
{
    vm = ptr;
}
void receiverFromView::exec(string str)
{
    if(str == "a has been set")
    {
        
    }
    /* ....... */

};
